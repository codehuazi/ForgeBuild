# ForgeBuild Day12 笔记：内容寻址本地编译缓存

## 1. Day12 目标

Day12 的核心目标是给 ForgeBuild 增加：

**Content-Addressable Local Compilation Cache（内容寻址本地编译缓存）**

解决的问题：

原有增量构建只能判断当前输出文件是否还存在、输入是否更新。

例如：

```text
a.cpp
  ↓
a.o
```

如果 `a.o` 被删除：

```bash
rm a.o
```

即使：

```text
a.cpp 没变
a.hpp 没变
编译命令没变
```

原来的 ForgeBuild 仍然必须重新调用 `g++`。

加入缓存后目标变成：

```text
以前编译过完全相同的输入
        ↓
     Cache Key
        ↓
缓存中存在对应 .o
        ↓
直接 restore
        ↓
跳过 g++
```

第一版范围：

```text
Linux
本地缓存
单输出 compile Edge
有 depfile 的 C/C++ 编译任务
```

暂时不做：

```text
link cache
remote cache
distributed cache
LRU
容量限制
Windows
```

---

# 2. Cache Key 设计

## 2.1 为什么不能只用输出文件名

错误方案：

```text
a.o → cache/a.o
```

因为：

```text
-O0 编译出来的 a.o
-O2 编译出来的 a.o
```

文件名完全一样，但二进制内容可能不同。

所以缓存对象必须由“输入身份”决定。

最终概念：

```text
Cache Key
=
cache format version
+ expanded command
+ dependency path
+ dependency content
+ dependency path
+ dependency content
+ ...
```

例如：

```text
ForgeBuildCacheV1
+
g++ -std=c++20 -c a.cpp -o a.o
+
a.cpp
+
content(a.cpp)
+
a.hpp
+
content(a.hpp)
```

然后经过 Hash：

```text
所有输入信息
   ↓
Hash64
   ↓
Cache Key
```

---

# 3. Hash64：增量流式哈希

原来只有：

```cpp
hash_string(text);
```

Day12 将 Hash 抽象成：

```cpp
forge::Hash64 hasher;

hasher.update(part1);
hasher.update(part2);
hasher.update(part3);

auto value = hasher.value();
```

使用：

```text
FNV-1a 64-bit
```

核心性质：

```text
hash(A + B + C)

==

update(A)
update(B)
update(C)
```

实际验证：

```text
command1 hash:
14425504672841978440

incremental hash:
14425504672841978440
```

两者完全相同。

### 为什么需要增量 Hash

如果 Cache Key 直接：

```cpp
std::string huge;

huge += command;
huge += source;
huge += header1;
huge += header2;
...
```

会产生：

```text
大字符串
多次内存扩容
多次拷贝
无法很好处理大文件
```

流式方式：

```text
command
  ↓
update()

source chunk
  ↓
update()

header chunk
  ↓
update()
```

内存占用基本固定。

---

# 4. 文件内容流式 Hash

增加：

```cpp
bool Hash64::update_file(
    const std::string& path
);
```

实现思想：

```text
ifstream(binary)
      ↓
4096-byte buffer
      ↓
read()
      ↓
gcount()
      ↓
update(valid bytes)
```

例如文件：

```text
10000 bytes
```

buffer：

```text
4096 bytes
```

读取过程：

```text
4096
4096
1808
```

Hash 实际接收到的仍然正好是完整的：

```text
10000 bytes
```

---

## 4.1 为什么使用 binary mode

内容寻址关注：

```text
真实字节内容
```

而不是文本语义。

因此：

```cpp
std::ios::binary
```

明确表示：

```text
按字节读取
```

---

## 4.2 文件读取失败

如果：

```text
a.hpp 不存在
```

必须：

```cpp
update_file(...)
→ false
```

不能把：

```text
不存在的文件
```

误认为：

```text
空文件
```

还验证了：

```text
Hash 当前状态
↓
update_file(不存在文件)
↓
返回 false
↓
Hash 状态不改变
```

---

# 5. CacheKeyBuilder

为了避免把 Cache Key 规则全部堆进 Executor，单独建立：

```text
CacheKeyBuilder
```

职责：

```text
version
command
dependencies
dependency paths
dependency contents
    ↓
Cache Key
```

Executor 不负责定义：

```text
“哪些字段决定缓存身份”
```

Executor 只负责使用 CacheKeyBuilder。

这是典型的职责拆分：

```text
Executor
→ 执行任务

CacheKeyBuilder
→ 定义缓存身份

LocalCache
→ 保存 / 恢复缓存对象
```

---

# 6. Cache Format Version

CacheKeyBuilder 初始化时加入：

```text
ForgeBuildCacheV1
```

意义：

以后 Cache Key 算法升级：

```text
V1:
command + deps

V2:
compiler identity + command + deps
```

只需要：

```text
ForgeBuildCacheV1
→ ForgeBuildCacheV2
```

旧缓存自然全部 miss。

不需要复杂缓存迁移。

---

# 7. Cache Key 字段边界

不能简单：

```cpp
hasher.update(field1);
hasher.update(field2);
```

否则：

```text
field1 = "ab"
field2 = "c"
```

和：

```text
field1 = "a"
field2 = "bc"
```

输入字节都是：

```text
abc
```

这不是普通 Hash Collision。

而是：

```text
结构编码本身发生歧义
```

因此加入字段标签和 `\0` 分隔：

```text
version
\0
ForgeBuildCacheV1
\0

command
\0
g++ ...
\0
```

注意 C++ 中：

```cpp
std::string_view("\0")
```

长度可能为 0。

所以使用：

```cpp
std::string_view(
    "\0",
    1
);
```

明确加入一个真实的零字节。

---

# 8. Dependency 内容进入 Cache Key

CacheKeyBuilder 增加：

```cpp
bool add_dependency(
    const std::string& path
);
```

加入的信息：

```text
dependency_path
+
dependency_content
```

为什么路径也加入？

假设：

```text
a.hpp
b.hpp
```

内容碰巧相同。

只 Hash 内容的话：

```text
content(a.hpp)
==
content(b.hpp)
```

无法表达“依赖的是哪个文件”。

因此 dependency 身份应该是：

```text
path + content
```

---

## 8.1 Header 内容变化测试

控制变量：

```text
Cache version     相同
command           相同
dependency path   相同
```

只修改：

```text
constexpr int value = 10;

↓

constexpr int value = 20;
```

结果：

```text
dependency key1:
15033089226173651803

dependency key2:
5473769748896575858
```

证明：

```text
dependency 内容变化
→ Cache Key 变化
```

---

# 9. LocalCache

建立：

```text
LocalCache
```

主要接口：

```cpp
bool contains(
    std::uint64_t key
) const;

bool store(
    std::uint64_t key,
    const std::string& output
);

bool restore(
    std::uint64_t key,
    const std::string& output
) const;
```

职责：

```text
Cache Key
   ↓
磁盘缓存对象
```

---

# 10. Content-Addressable Storage

普通缓存：

```text
a.o
→ cache/a.o
```

内容寻址缓存：

```text
输入状态
  ↓
Cache Key
  ↓
3db15200fcb15c31
```

磁盘：

```text
.forge_cache/
└── objects/
    ├── 3db15200fcb15c31
    ├── c5212f20e0ee37c5
    └── d753a6fcb7cb77c0
```

即：

```text
对象位置由内容身份决定
```

而不是：

```text
对象名字决定位置
```

---

# 11. LocalCache Store / Restore

第一次测试流程：

```text
创建 fake object
        ↓
store(key)
        ↓
删除原 output
        ↓
contains(key)
        ↓
restore(key)
        ↓
output 恢复
        ↓
检查内容完全一致
```

验证结果：

```text
local cache store/restore passed
```

说明最基本 Cache 存取闭环完成。

---

# 12. Executor 接入 Cache

Executor 原流程：

```text
edge->command()
      ↓
system(g++)
      ↓
depfile
      ↓
DepsLog
      ↓
BuildLog
```

加入缓存后：

```text
edge->command()
      ↓
是否 cacheable？
      ↓
是否已有 DepsLog？
      ↓
计算 Cache Key
      ↓
LocalCache lookup
   ┌───────┴────────┐
   │                │
 hit               miss
   │                │
restore           g++
   │                │
return true       depfile
                    ↓
                 DepsLog
                    ↓
                Cache Key
                    ↓
                  store
```

---

# 13. 哪些 Edge 可以缓存

第一版判断：

```text
LocalCache 存在
+
DepsLog 存在
+
有 depfile
+
只有一个 output
```

因此现在主要缓存：

```text
compile Edge
```

不缓存：

```text
link Edge
multi-output Edge
普通 shell Edge
```

这是主动控制 MVP 范围，而不是功能缺陷。

---

# 14. 首次构建没有 depfile：Chicken-and-Egg 问题

这是 Day12 最值得面试讲的问题之一。

第一次编译：

```text
a.cpp 已知
```

但是：

```cpp
#include "a.hpp"
```

真正用了哪些 Header，是编译器执行后才通过 depfile 知道。

所以第一次编译前：

```text
DepsLog 中没有 a.o
```

无法计算完整 Cache Key。

解决方案：

```text
第一次：

没有 DepsLog
    ↓
直接 compile
    ↓
生成 depfile
    ↓
解析动态依赖
    ↓
DepsLog
    ↓
计算完整 Cache Key
    ↓
store
```

之后：

```text
已有 DepsLog
    ↓
编译前即可获得完整 dependency 集合
    ↓
计算 Cache Key
    ↓
lookup cache
```

---

# 15. 第一次真实缓存构建

真实工程：

```text
a.cpp    → a.o ───┐
b.cpp    → b.o ───┼→ app
main.cpp → main.o ─┘
```

第一次构建：

```text
planned edge count: 4
```

三个 compile：

```text
cache miss
→ g++
→ cache store
```

实际：

```text
cache store: a.o
cache store: b.o
cache store: main.o
```

link：

```text
g++ a.o b.o main.o -o app
```

最后：

```bash
./app
```

输出：

```text
30
```

---

# 16. 删除 `.o` 后 Cache Hit

删除：

```text
a.o
b.o
main.o
app
```

保留：

```text
.forge_cache
.forge_deps
```

重新构建。

结果：

```text
cache hit: a.o
cache hit: b.o
cache hit: main.o
```

没有再次执行：

```text
g++ -c a.cpp
g++ -c b.cpp
g++ -c main.cpp
```

只重新执行：

```text
link
```

最终：

```text
./app
→ 30
```

这证明了真正的：

```text
Cache Key
→ LocalCache
→ restore .o
→ 跳过 compiler
```

---

# 17. Header 修改导致 Cache Miss

测试：

```bash
echo "// cache invalidation test" >> a.hpp
```

依赖关系：

```text
a.cpp    → a.hpp
main.cpp → a.hpp

b.cpp    → 不依赖 a.hpp
```

构建结果：

```text
planned edge count: 3
```

只执行：

```text
a.cpp    → a.o
main.cpp → main.o
app      → link
```

`b.o` 不受影响。

而且：

```text
a.o     → cache miss
main.o  → cache miss
```

说明：

```text
a.hpp 内容变化
→ Cache Key 改变
→ 不会错误命中旧 .o
```

最终：

```text
./app
→ 30
```

---

# 18. 原子 Cache 写入

最初：

```text
output
↓
直接 copy 到最终 cache object
```

有风险：

```text
进程写到一半崩溃
↓
最终 object 文件已经存在
↓
留下半个缓存对象
```

改成：

```text
output
   ↓
temp object
   ↓
完整写入
   ↓
rename
   ↓
final object
```

临时文件名包含：

```text
PID + atomic counter
```

避免并行 Worker 使用相同 temp 文件。

---

# 19. Metadata 与 Cache 损坏检测

每个缓存对象现在包含：

```text
3db15200fcb15c31
3db15200fcb15c31.meta
```

`.meta` 保存：

```text
object size
object content hash
```

restore 前：

```text
读取 metadata
      ↓
检查 file size
      ↓
重新 Hash cache object
      ↓
比较 expected hash
      ↓
全部一致才允许 restore
```

---

# 20. 为什么 metadata 最后提交

Store：

```text
temp object
temp metadata
     ↓
rename object
     ↓
rename metadata
```

Metadata 最后写入意味着：

```text
object + metadata 都存在
```

才代表一个完整缓存条目。

可以把：

```text
metadata
```

理解成缓存条目的：

```text
commit marker
```

---

# 21. Restore 也使用临时文件

恢复时没有直接：

```text
cache object
→ output
```

而是：

```text
cache object
     ↓
temp output
     ↓
rename
     ↓
正式 output
```

防止 restore 过程中出错留下半个目标文件。

---

# 22. Cache 损坏回退

人为把缓存对象改成：

```text
corrupted
```

保留 `.meta`。

然后删除：

```text
a.o
b.o
main.o
app
```

重新构建。

LocalCache：

```text
contains()
→ true

restore()
→ size/hash 不匹配
→ 删除坏 cache
→ false
```

Executor：

```text
restore false
→ 不把 build 判失败
→ fallback 到正常 g++
```

实际结果：

```text
cache miss
→ g++
→ cache store
```

三个 `.o` 全部重新生成。

最终：

```text
build succeeded
./app
→ 30
```

---

# 23. Day12 最重要的工程原则

## 原则 1：Cache 只能影响性能，不能影响正确性

```text
cache hit
→ 加速

cache miss
→ 正常编译

cache 损坏
→ 正常编译

cache 不可读取
→ 正常编译
```

绝不能：

```text
Cache 出问题
→ 构建结果错误
```

---

## 原则 2：Cache Key 必须描述完整输入身份

目前第一版：

```text
cache format version
+
expanded command
+
dependency path
+
dependency content
```

未来工业级还应该加入：

```text
compiler path
compiler version
environment
relevant flags
target platform
```

但第一版没有过度复杂化。

---

## 原则 3：Timestamp 用于增量判断，Content 用于内容寻址

Builder dirty 判断：

```text
mtime / output existence
```

Cache Key：

```text
file content
```

例如：

```bash
touch a.cpp
```

文件时间变了：

```text
Builder
→ 可能判断需要重新执行 Edge
```

但是内容没变：

```text
Cache Key
→ 可以仍然相同
→ cache hit
```

这体现：

```text
Incremental Build
和
Content Cache
```

是两个不同层次的优化。

---

# 24. Day12 模块关系

最终核心结构：

```text
                    Builder
                      │
                      ▼
                  BuildPlan
                      │
                      ▼
                  Scheduler
                      │
                      ▼
                   Executor
                  /        \
                 /          \
        CacheKeyBuilder    LocalCache
              │               │
              │               ├─ contains
              │               ├─ store
              │               └─ restore
              │
              ▼
            Hash64
              │
              ├─ update()
              └─ update_file()

Executor
   │
   ├── BuildLog
   │
   └── DepsLog
          │
          ▼
      dynamic headers
```

---

# 25. Day12 面试高频问题

## Q1：你的 Cache Key 包含什么？

答：

ForgeBuild 第一版 Cache Key 包含缓存格式版本、展开后的编译命令，以及 depfile 中记录的每个动态依赖文件路径和文件内容。这样编译选项、源文件或者头文件内容变化都会产生新的 Key。

---

## Q2：为什么 Header 必须参与 Cache Key？

答：

C/C++ 编译结果不仅由 `.cpp` 决定，也受到所有实际包含头文件的影响。如果只 Hash `.cpp`，头文件修改后就可能错误恢复旧 `.o`。因此我利用 depfile 获取编译器实际使用的动态依赖，并将这些依赖内容加入 Cache Key。

---

## Q3：第一次构建没有 depfile，怎么查缓存？

答：

第一次无法获得完整动态依赖，因此不尝试 cache hit，而是正常执行编译。编译完成后解析 depfile，将依赖保存到 DepsLog，然后计算完整 Cache Key 并存储目标文件。以后再次构建时可以利用持久化的 DepsLog 在编译前计算 Key。

---

## Q4：为什么不用 timestamp 做 Cache Key？

答：

Timestamp 代表文件什么时候被修改，不代表内容是什么。内容寻址缓存关注输入内容。如果只是 `touch` 文件但内容没变，理想情况下 Cache Key 应保持不变，因此 Cache Key 应基于内容，而不是 mtime。

---

## Q5：缓存对象损坏怎么办？

答：

我给每个缓存对象保存 size 和内容 Hash metadata。restore 前先验证大小和 Hash，如果发现损坏就删除无效对象并返回 cache miss，由 Executor 自动回退到正常编译路径，再重新写入缓存，因此缓存损坏不会影响构建正确性。

---

## Q6：为什么使用临时文件 + rename？

答：

直接向最终缓存路径写数据，如果过程中进程崩溃，可能留下半写入对象。我的实现先写临时对象，完整写入后再 rename 到最终路径，利用同一文件系统下 rename 的原子提交语义降低产生不完整缓存的风险。

---

## Q7：为什么单独设计 CacheKeyBuilder？

答：

Executor 的职责应该是执行任务和控制 cache hit/miss，而 Cache Key 的字段规则属于缓存身份定义。将其抽成 CacheKeyBuilder 后，Key 规则不会散落在 Executor 中，以后增加 compiler identity 或 CacheV2 时也更容易修改。

---

## Q8：为什么第一版不缓存 link？

答：

这是主动做 MVP 范围控制。Compile Edge 有稳定的单输出和 depfile 动态依赖信息，最适合先验证缓存机制。Link 的输入、命令和其他语义以后可以用同样框架扩展，但没有必要在第一版同时增加复杂度。

---

# 26. Day12 可以写进简历的内容

可以概括为：

> 实现基于内容寻址的本地编译缓存，设计 CacheKeyBuilder，将编译命令及 depfile 动态依赖路径/文件内容纳入 Cache Key；支持缓存命中恢复目标文件，并通过临时文件 + rename 实现原子写入，结合 size/hash metadata 检测缓存损坏并自动回退编译。

更精简：

> 设计内容寻址本地编译缓存，基于编译命令与 depfile 动态依赖内容生成 Cache Key，实现 cache hit 恢复、原子写入及损坏检测与编译回退。

---

# 27. Day12 当前完成状态

已经完成：

```text
✓ Hash64 增量哈希
✓ 文件内容流式 Hash
✓ CacheKeyBuilder
✓ command 进入 Cache Key
✓ dependency path/content 进入 Key
✓ LocalCache store/restore
✓ Executor cache hit/miss
✓ 首次构建 store
✓ 删除 .o 后 restore
✓ Header 内容变化 cache miss
✓ 原子 cache 写入
✓ Metadata
✓ 缓存损坏检测
✓ 损坏后 fallback compile
```

当前尚未完成的 Day12 收口工作：

```text
□ 将 hash / LocalCache 关键测试正式注册进 CTest
□ 做缓存相关 benchmark
□ 整理最终性能数据
```

这几项属于工程化收尾，不再改变 Cache 核心架构。

---

# 28. Day12 一句话总结

> ForgeBuild 在原有增量构建基础上进一步实现了内容寻址本地编译缓存：利用 DepsLog 获取编译器真实动态依赖，通过编译命令、依赖路径和依赖文件内容生成 Cache Key；缓存命中时恢复目标文件并跳过编译器，同时通过原子写入和内容校验保证缓存异常只影响性能、不影响构建正确性。
