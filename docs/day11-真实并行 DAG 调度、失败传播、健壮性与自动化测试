# ForgeBuild Day11 完整总结笔记

## 主题：真实并行 DAG 调度、失败传播、健壮性与自动化测试

---

# 一、Day11 的最终目标

Day11 的核心目标不是简单地“开几个线程执行命令”，而是实现一个真正满足构建依赖约束的并行调度器：

```text
Builder 生成本次需要执行的最小构建子图
                  ↓
Scheduler 根据 DAG 依赖关系调度 Edge
                  ↓
多个 Worker 并行执行无依赖任务
                  ↓
前置任务完成后动态释放下游任务
                  ↓
任意任务失败时停止整个构建
```

最终 ForgeBuild 已经支持：

```bash
./build/debug/forge -j 3 build.forge
```

并完成了以下能力：

* 支持 `-j N` 和 `-jN`；
* 多个独立编译任务真正并行执行；
* link 必须等待所有依赖编译完成；
* 与增量构建结合，只调度最小受影响子图；
* 编译失败后阻止下游任务；
* 正确唤醒 Worker 和主线程，避免失败路径死锁；
* 防御非法线程数和未设置 Executor；
* 正确处理“单个 Edge 多输出”的依赖计数；
* 使用 CTest 自动测试；
* 给并发测试设置超时；
* 用计时实验验证真实并行加速。

---

# 二、Day11 最终执行架构

当前 ForgeBuild 的整体构建链路可以概括为：

```text
build.forge
    ↓
Parser
    ↓
Manifest + BuildGraph
    ↓
Builder / BuildPlanner
    ↓
BuildPlan
    ↓
Scheduler
    ↓
Executor
    ↓
真实 shell / g++ 命令
```

各模块职责如下。

## 1. Parser

负责读取：

```text
rule compile
 command = g++ ...
 depfile = $out.d

build a.o: compile a.cpp
```

并构建：

```text
Rule
Node
Edge
BuildGraph
```

## 2. Builder

负责判断：

```text
哪些 Edge 需要重新执行
```

例如只修改：

```bash
touch a.cpp
```

Builder 生成的最小计划为：

```text
compile a
link app
```

不会把 `compile b` 和 `compile main` 放进计划。

## 3. Scheduler

负责判断：

```text
计划中的任务以什么顺序执行
哪些任务可以同时执行
```

## 4. Executor

负责真正运行：

```bash
g++ -c ...
g++ ... -o app
```

所以一句话总结：

> Builder 决定“做什么”，Scheduler 决定“怎么并行做”，Executor 负责“真正执行”。

---

# 三、命令行并行度参数

Day11 增加了：

```bash
forge -j 4 build.forge
forge -j4 build.forge
```

同时保留：

```bash
forge build.forge
```

默认：

```text
jobs = 1
```

---

## 1. 支持形式

```bash
./forge build.forge
./forge -j 4 build.forge
./forge -j4 build.forge
```

## 2. 非法输入

以下参数会被拒绝：

```bash
-j 0
-j -1
-j abc
-j
--unknown
```

## 3. 参数解析重点

不能只调用：

```cpp
std::stoi(value)
```

因为：

```cpp
std::stoi("4abc")
```

可能会成功解析前面的 `4`。

因此需要检查完整消费长度：

```cpp
std::size_t position = 0;

int jobs = std::stoi(
    text,
    &position
);

if (position != text.size())
{
    // 非法
}
```

还要检查：

```cpp
jobs > 0
```

---

# 四、CLI 接入 Scheduler

原来 CLI 可能直接调用：

```cpp
executor.execute(plan);
```

Day11 改为：

```cpp
forge::Executor executor(
    build_log,
    deps_log
);

forge::Scheduler scheduler(
    jobs
);

scheduler.set_executor(
    &executor
);

if (!scheduler.run(
        plan.edges()
    ))
{
    std::cerr
        << "build failed\n";

    return 1;
}
```

这样：

```text
-j 1
-j 3
-j 8
```

都走同一套 Scheduler 路径。

这比维护：

```text
串行 Executor 路径
并行 Scheduler 路径
```

两套代码更可靠。

---

# 五、Scheduler 的核心数据结构

Scheduler 主要维护：

```cpp
int jobs_;

Executor* executor_;
```

任务状态：

```cpp
std::unordered_map<
    Edge*,
    int
> indegree_;

std::queue<Edge*> ready_queue_;
```

线程同步：

```cpp
std::mutex mutex_;

std::condition_variable condition_;

std::condition_variable finished_condition_;
```

运行状态：

```cpp
std::size_t total_edges_;

std::size_t completed_edges_;

std::size_t running_edges_;

bool stopping_;

std::atomic<bool> failed_;
```

输出同步：

```cpp
std::mutex output_mutex_;
```

---

# 六、Scheduler 的基本调度模型

## 1. 入度

对于一个 Edge：

```text
a.o + b.o + main.o → link → app
```

link 依赖三个 compile Edge：

```text
compile a
compile b
compile main
```

所以：

```text
link indegree = 3
```

## 2. Ready Queue

初始入度为零的 Edge 可以立即执行：

```text
compile a
compile b
compile main
```

它们进入：

```cpp
ready_queue_
```

link 不进入，因为：

```text
indegree = 3
```

## 3. 动态释放

每完成一个前置任务：

```text
3 → 2
2 → 1
1 → 0
```

当 link 入度变为 0 时：

```cpp
ready_queue_.push(
    link_edge
);
```

然后唤醒 Worker。

---

# 七、Worker 线程的执行流程

Worker 的基本循环：

```text
启动
 ↓
等待 Ready Queue
 ↓
领取 Edge
 ↓
执行命令
 ↓
成功：完成 Edge 并释放下游
失败：通知全局停止
 ↓
继续等待或退出
```

伪代码：

```cpp
while (true)
{
    Edge* edge = nullptr;

    {
        std::unique_lock lock(
            mutex_
        );

        condition_.wait(
            lock,
            [this]()
            {
                return stopping_
                    || failed_
                    || !ready_queue_.empty();
            }
        );

        if (stopping_
            || failed_)
        {
            break;
        }

        edge =
            ready_queue_.front();

        ready_queue_.pop();

        running_edges_++;
    }

    const bool success =
        executor_->execute_edge(
            edge
        );

    if (!success)
    {
        // 设置失败并通知其他线程
        return;
    }

    finish_edge(
        edge
    );

    condition_.notify_all();
}
```

---

# 八、为什么任务执行不能持有 Scheduler 锁

Worker 领取任务时需要锁：

```cpp
mutex_
```

但是执行：

```cpp
executor_->execute_edge(edge)
```

时不能继续持有这把锁。

错误写法：

```cpp
std::lock_guard lock(
    mutex_
);

executor_->execute_edge(
    edge
);
```

这会导致：

```text
Worker 1 执行 g++ 时一直占着 Scheduler 锁
Worker 2 无法领取任务
Worker 3 无法领取任务
```

最终虽然有多个线程，却退化成串行。

正确做法：

```text
锁内：
领取任务
更新 running 数量

锁外：
执行耗时命令

锁内：
更新完成状态
释放下游
```

这叫：

> 缩小临界区。

---

# 九、真实多源文件并行验证

测试项目：

```text
a.cpp
b.cpp
main.cpp
```

构建规则：

```text
build a.o: compile a.cpp
build b.o: compile b.cpp
build main.o: compile main.cpp
build app: link a.o b.o main.o
```

正确 DAG：

```text
a.cpp    → a.o ───┐
b.cpp    → b.o ───┼→ app
main.cpp → main.o ─┘
```

运行：

```bash
./build/debug/forge -j 3 build.forge
```

正确输出特征：

```text
worker picked: a.cpp --compile--> a.o
worker picked: b.cpp --compile--> b.o
worker picked: main.cpp --compile--> main.o
```

三个编译任务可以同时执行。

必须在三个编译全部完成后，才能看到：

```text
worker picked:
a.o + b.o + main.o --link--> app
```

最后：

```bash
./app
```

输出：

```text
30
```

---

# 十、Parser 双向建图 Bug

第一次真实多任务测试时出现：

```text
finished: b.cpp --compile--> b.o
worker picked:
a.o + b.o + main.o --link--> app
```

此时 `main.o` 还没有生成，导致：

```text
/usr/bin/ld: 找不到 main.o
```

---

## 1. 根本原因

Parser 原来直接调用：

```cpp
edge->add_input(
    node
);

edge->add_output(
    node
);
```

这只更新了 Edge 一侧：

```text
Edge.inputs_
Edge.outputs_
```

没有更新 Node 一侧：

```text
Node.in_edge_
Node.out_edges_
```

Scheduler 依赖：

```cpp
input->in_edge()
```

查找输入文件的生产者。

由于 `Node::in_edge_` 没有设置，Scheduler 得到：

```cpp
nullptr
```

于是错误认为 link 没有前置任务：

```text
link indegree = 0
```

---

## 2. 正确修复

Parser 必须通过 BuildGraph 接口：

```cpp
manifest.graph()
        .add_input(
            edge,
            input_node
        );
```

以及：

```cpp
manifest.graph()
        .add_output(
            edge,
            output_node
        );
```

因为：

```cpp
BuildGraph::add_input(
    Edge* edge,
    Node* node
)
{
    edge->add_input(
        node
    );

    node->add_out_edge(
        edge
    );
}
```

以及：

```cpp
BuildGraph::add_output(
    Edge* edge,
    Node* node
)
{
    edge->add_output(
        node
    );

    node->set_in_edge(
        edge
    );
}
```

---

## 3. 核心结论

构建图必须维护双向关系：

```text
producer Edge
    ↓ outputs
Node
    ↓ inputs
dependent Edge
```

对应：

```text
producer.outputs_ 包含 Node
Node.in_edge_ 指向 producer

dependent.inputs_ 包含 Node
Node.out_edges_ 包含 dependent
```

只维护单侧，图就是残缺的。

---

# 十一、增量构建与并行调度结合

## 场景一：无修改

执行：

```bash
./build/debug/forge -j 3 build.forge
```

结果：

```text
planned edge count: 0
nothing to build
```

这说明 Builder 判断所有产物都是最新的。

---

## 场景二：修改公共头文件

执行：

```bash
touch a.hpp
```

由于：

```cpp
// a.cpp
#include "a.hpp"
```

以及：

```cpp
// main.cpp
#include "a.hpp"
```

所以受影响的输出是：

```text
a.o
main.o
```

计划：

```text
compile a
compile main
link app
```

结果：

```text
planned edge count: 3
```

其中：

```text
compile a
compile main
```

可以并行。

`compile b` 不需要执行。

---

## 场景三：只修改 a.cpp

执行：

```bash
touch a.cpp
```

计划：

```text
compile a
link app
```

结果：

```text
planned edge count: 2
```

没有执行：

```text
compile b
compile main
```

---

## 核心认识

```text
Worker 数量
```

只是并行上限，不代表一定能同时跑那么多任务。

例如：

```text
compile a
    ↓
link app
```

即使：

```bash
-j 3
```

也只能一次执行一个任务，因为 DAG 本身是串行链。

---

# 十二、失败传播

测试中故意让 `a.cpp` 编译失败：

```cpp
int value_a()
{
    return does_not_exist;
}
```

正确行为：

```text
compile a 失败
    ↓
a.o 不会成功生成
    ↓
link app 不执行
    ↓
Scheduler 返回 false
    ↓
CLI 输出 build failed
    ↓
进程退出码为 1
```

实际验证：

```bash
echo $?
```

得到：

```text
1
```

---

# 十三、失败路径死锁 Bug

第一次失败测试时出现：

```text
command failed: ...
worker stopped
worker stopped
```

然后程序卡住。

---

## 1. 原因

Worker 失败时设置：

```cpp
failed_ = true;
stopping_ = true;
```

并通知：

```cpp
condition_.notify_all();
```

这会唤醒其他 Worker。

但主线程等待的是：

```cpp
finished_condition_.wait(
    lock,
    [this]()
    {
        return failed_
            || completed_edges_ == total_edges_;
    }
);
```

主线程睡在：

```cpp
finished_condition_
```

上。

失败分支没有通知它，所以：

```text
failed_ 已经变成 true
但主线程没有醒来检查谓词
```

导致永久等待。

---

## 2. 正确修复

失败时同时通知两个条件变量：

```cpp
condition_.notify_all();

finished_condition_.notify_all();
```

职责：

```text
condition_
→ 唤醒其他 Worker，让它们停止

finished_condition_
→ 唤醒 run() 主线程，让它返回 false
```

---

## 3. 条件变量的重要规则

只修改状态：

```cpp
failed_ = true;
```

不等于线程会自动醒来。

必须同时：

```text
修改等待谓词依赖的状态
通知正在等待对应条件变量的线程
```

---

# 十四、两个条件变量的职责

## 1. `condition_`

Worker 等待它。

用于通知：

```text
Ready Queue 有新任务
Scheduler 要停止
构建已经失败
```

## 2. `finished_condition_`

主线程等待它。

用于通知：

```text
有任务完成
全部任务完成
某个任务失败
```

失败时两者都必须通知。

---

# 十五、Scheduler 内部防御

CLI 已经拒绝：

```bash
-j 0
```

但 Scheduler 不能完全信任调用者。

---

## 1. `jobs <= 0`

如果直接调用：

```cpp
Scheduler scheduler(
    0
);
```

然后进入 `run()`：

```text
创建 0 个 Worker
主线程等待任务完成
没有任何线程执行任务
永久死锁
```

所以在 `run()` 开头检查：

```cpp
if (jobs_ <= 0)
{
    std::cerr
        << "scheduler requires jobs > 0\n";

    return false;
}
```

---

## 2. 未设置 Executor

错误调用：

```cpp
Scheduler scheduler(
    2
);

scheduler.run(
    edges
);
```

如果 Worker 原来写成：

```cpp
bool success = true;

if (executor_ != nullptr)
{
    success =
        executor_->execute_edge(
            edge
        );
}
```

那么空 Executor 会导致：

```text
命令没有执行
success 仍然是 true
Edge 被假装完成
Scheduler 返回成功
```

这是非常危险的假成功。

所以 `run()` 开头检查：

```cpp
if (executor_ == nullptr)
{
    std::cerr
        << "scheduler executor is not set\n";

    return false;
}
```

然后 Worker 可以直接写：

```cpp
const bool success =
    executor_->execute_edge(
        edge
    );
```

---

## 3. 工程原则

```text
CLI：
负责友好处理用户输入

核心模块：
负责保护自己的合法状态
```

这属于：

> Fail fast，尽早失败。

---

# 十六、Scheduler 防御测试

创建：

```text
scheduler_guard_demo
```

测试两种非法情况：

```text
jobs == 0
未设置 Executor
```

期望：

```text
zero jobs result: false
missing executor result: false
scheduler guard test passed
```

同时测试命令：

```bash
echo scheduler guard demo
```

不能真正执行。

这验证了错误是在 Worker 启动之前被发现的。

---

# 十七、任务依赖去重问题

Scheduler 最初按输入 Node 数量计算入度：

```cpp
for (Node* input : edge->inputs())
{
    Edge* producer =
        input->in_edge();

    if (producer 在本次计划中)
    {
        indegree_[edge]++;
    }
}
```

这在单输出场景通常没问题。

但考虑一个合法场景：

```text
generator
 ├── generated.cpp
 └── generated.hpp
```

下游同时输入两个文件：

```text
generated.cpp ─┐
generated.hpp ─┴→ consume
```

文件关系有两条：

```text
generator → generated.cpp → consume
generator → generated.hpp → consume
```

但任务关系只有一条：

```text
generator → consume
```

所以 consume 的任务入度应该是：

```text
1
```

而不是：

```text
2
```

---

# 十八、入度计算按 Producer Edge 去重

使用：

```cpp
std::unordered_set<Edge*> unique_producers;
```

正确逻辑：

```cpp
for (auto* edge : edges)
{
    std::unordered_set<Edge*>
        unique_producers;

    for (auto* input :
         edge->inputs())
    {
        Edge* producer =
            input->in_edge();

        if (producer == nullptr)
        {
            continue;
        }

        if (!indegree_.contains(
                producer
            ))
        {
            continue;
        }

        unique_producers.insert(
            producer
        );
    }

    indegree_[edge] =
        static_cast<int>(
            unique_producers.size()
        );
}
```

同一个 producer 即使通过多个输出 Node 被找到多次，集合中也只有一个。

---

# 十九、完成释放按 Dependent Edge 去重

只修改初始化入度还不够。

生产者完成时会遍历：

```text
所有 output Node
每个 output 的 out_edges
```

对于多输出场景，同一个 dependent 可能被找到两次。

所以也要：

```cpp
std::unordered_set<Edge*>
    unique_dependents;
```

正确逻辑：

```cpp
for (auto* output :
     edge->outputs())
{
    for (auto* dependent :
         output->out_edges())
    {
        if (!indegree_.contains(
                dependent
            ))
        {
            continue;
        }

        unique_dependents.insert(
            dependent
        );
    }
}

for (auto* dependent :
     unique_dependents)
{
    indegree_[dependent]--;

    if (indegree_[dependent] == 0)
    {
        ready_queue_.push(
            dependent
        );
    }
}
```

---

# 二十、为什么两边必须对称去重

初始化：

```text
同一个 producer 只计一次
```

释放：

```text
同一个 dependent 只减一次
```

必须保持对称。

否则如果初始化去重为 1，但释放两次：

```text
1 → 0 → -1
```

会造成错误状态。

---

# 二十一、多输出专项测试

测试 DAG：

```text
generate
 ├── generated.cpp
 └── generated.hpp
          ↓
       consume
          ↓
       result.txt
```

运行时正确顺序：

```text
generate
    ↓
两个文件生成
    ↓
consume
```

最终：

```text
scheduler result: true
```

没有出现：

* consume 提前执行；
* consume 重复执行；
* 入度无法归零；
* Scheduler 卡死。

---

# 二十二、CTest 接入

原来的 `*_demo` 只是普通程序：

```cmake
add_executable(...)
```

需要人工运行和观察。

Day11 加入：

```cmake
include(CTest)
```

然后注册测试：

```cmake
add_test(
    NAME scheduler_guard
    COMMAND scheduler_guard_demo
)
```

---

## 1. CTest 判断规则

CTest 主要根据退出码判断：

```text
返回 0
→ Passed

返回非 0
→ Failed
```

因此测试程序必须把断言结果映射到退出码。

例如：

```cpp
if (!test_passed)
{
    return 1;
}

return 0;
```

---

## 2. 当前三个测试

```text
scheduler_guard
scheduler_multi_output
scheduler_failure
```

查看测试列表：

```bash
ctest \
    --test-dir build/debug \
    -N
```

运行所有测试：

```bash
ctest \
    --test-dir build/debug \
    --output-on-failure
```

详细输出：

```bash
ctest \
    --test-dir build/debug \
    -V
```

只运行一个测试：

```bash
ctest \
    --test-dir build/debug \
    -R scheduler_failure \
    -V
```

---

# 二十三、强化多输出测试：不能只看日志

最初多输出测试只检查：

```text
Scheduler 返回 true
```

但并发程序不能仅凭日志顺序判断正确性。

原因包括：

* `std::cout` 有缓冲；
* shell 子进程直接写标准输出；
* CTest 捕获多个输出来源；
* 多线程输出顺序可能交错。

因此强化测试，检查真实副作用：

```text
generated.cpp 存在
generated.hpp 存在
result.txt 存在
result.txt 内容为 consumed
```

consume 命令：

```bash
sh -c '
    test -f generated.cpp &&
    test -f generated.hpp &&
    echo consumed > result.txt
'
```

如果 consume 被提前执行：

```text
输入文件不存在
→ test -f 失败
→ 命令返回非零
→ Scheduler 返回 false
→ CTest 失败
```

最终测试输出：

```text
scheduler result: true
files exist: true
content correct: true
scheduler multi-output test passed
```

---

# 二十四、失败传播自动化测试

测试图：

```text
failing_edge
    ↓
failure_output.txt
    ↓
downstream_edge
    ↓
downstream_output.txt
```

上游命令故意失败：

```bash
sh -c '
    rm -f failure_output.txt &&
    exit 1
'
```

下游命令：

```bash
sh -c '
    echo should-not-run >
    downstream_output.txt
'
```

测试判定：

```cpp
const bool test_passed =
    !scheduler_result
    && !downstream_exists;
```

注意：

```text
被测构建失败
```

正是预期行为，所以：

```text
Scheduler 返回 false
下游未执行
→ 测试本身返回 0
→ CTest 显示 Passed
```

最终输出：

```text
scheduler result: false
downstream exists: false
scheduler failure propagation test passed
```

---

# 二十五、CTest 超时防死锁

CTest 默认超时显示：

```text
1500 秒
```

对于几毫秒级并发测试，太长。

在 CMake 中加入：

```cmake
set_tests_properties(
    scheduler_guard
    scheduler_multi_output
    scheduler_failure
    PROPERTIES
        TIMEOUT 5
)
```

验证：

```text
Test timeout computed to be: 5
```

这样发生死锁时：

```text
5 秒未结束
→ CTest 终止程序
→ 标记 Failed
```

避免 CI 挂住 25 分钟。

---

# 二十六、真实并行加速测试

为了排除小文件编译时间过短造成的测量波动，临时把 compile 命令改成：

```text
command =
sleep 1 &&
g++ -std=c++20
-MMD -MF $out.d
-c $in -o $out
```

三个互不依赖的编译任务，每个至少耗时 1 秒。

---

## 1. 串行测试

执行：

```bash
/usr/bin/time \
    -f 'elapsed: %e seconds' \
    ./build/debug/forge -j 1 build.forge
```

结果：

```text
elapsed: 3.42 seconds
```

执行关系：

```text
compile a
→ compile b
→ compile main
→ link
```

---

## 2. 并行测试

执行：

```bash
/usr/bin/time \
    -f 'elapsed: %e seconds' \
    ./build/debug/forge -j 3 build.forge
```

结果：

```text
elapsed: 1.39 seconds
```

执行关系：

```text
compile a ─┐
compile b ─┼→ 同时执行
compile m ─┘
     ↓
   link
```

---

## 3. 加速比

```text
speedup
= 串行时间 / 并行时间
= 3.42 / 1.39
≈ 2.46
```

即：

> `-j 3` 相比 `-j 1`，在当前测试中约快 2.46 倍。

---

## 4. 为什么达不到理论 3 倍

存在额外开销：

* `g++` 进程启动；
* shell 启动；
* mutex 加锁；
* 条件变量唤醒；
* 日志输出；
* link 仍然串行；
* 虚拟机和系统调度波动。

所以 2.46 倍属于正常结果。

---

# 二十七、最终正常构建验证

恢复正常 compile 命令后：

```text
command =
g++ -std=c++20
-MMD -MF $out.d
-c $in -o $out
```

执行：

```bash
rm -f \
    a.o a.o.d \
    b.o b.o.d \
    main.o main.o.d \
    app \
    .forge_log \
    .forge_deps

./build/debug/forge \
    -j 3 \
    build.forge

./app
```

结果：

```text
build succeeded
30
```

说明 Day11 的功能没有依赖测试用的 `sleep 1`。

---

# 二十八、你最后一次输出中的日志交错

你最后一次输出有一段：

```text
worker picked: g++ -std=c++20 ...
b.cpp --compile--> b.o
```

正常意图应该分别是：

```text
worker picked: b.cpp --compile--> b.o
g++ -std=c++20 ... b.cpp ...
```

这是多线程输出交错，不是任务执行错误。

---

## 1. 原因

Scheduler 使用自己的：

```cpp
output_mutex_
```

保护：

```text
worker picked
finished
worker started
worker stopped
```

Executor 可能使用另一把输出锁保护：

```text
g++ 命令
command failed
```

两把不同的 mutex 只能分别保证：

```text
Scheduler 内部日志不互相打断
Executor 内部日志不互相打断
```

不能保证：

```text
Scheduler 日志
和
Executor 日志
```

之间不会交错。

即：

```text
Scheduler output_mutex
≠ Executor output_mutex
```

---

## 2. 当前影响

只影响：

```text
日志可读性
```

不影响：

* DAG；
* 入度；
* Ready Queue；
* 文件生成；
* 命令执行；
* 构建结果。

因为最后仍然：

```text
build succeeded
30
```

---

## 3. 后续可优化方案

后续可以设计统一日志模块：

```cpp
class Logger
{
public:
    void line(
        const std::string& message
    );

private:
    std::mutex mutex_;
};
```

然后：

```text
Scheduler
Executor
CLI
```

共享同一个 Logger。

这样所有日志都使用同一把锁，可以保证一整行原子输出。

当前阶段不需要立即修改，因为 Day11 的核心是并行调度，不是日志系统。

---

# 二十九、Day11 中遇到的主要 Bug 总结

## Bug 1：Parser 只建立单向关系

现象：

```text
link 提前执行
找不到 main.o
```

原因：

```text
只更新 Edge.inputs/outputs
没有更新 Node.in_edge/out_edges
```

修复：

```text
统一使用 BuildGraph::add_input/add_output
```

---

## Bug 2：Worker 成功路径自死锁

此前阶段曾出现 Worker 持有 Scheduler mutex 后，再调用会重复加锁的 `finish_edge()`。

原因：

```text
同一非递归 mutex 被同一线程重复 lock
```

修复：

```text
执行任务和调用 finish_edge 前释放外层锁
```

---

## Bug 3：失败路径主线程不醒

现象：

```text
编译失败
两个 Worker 退出
程序永久卡住
```

原因：

```text
只通知 condition_
漏通知 finished_condition_
```

修复：

```cpp
condition_.notify_all();
finished_condition_.notify_all();
```

---

## Bug 4：jobs == 0 会死锁

原因：

```text
0 个 Worker
主线程等待任务完成
```

修复：

```cpp
if (jobs_ <= 0)
{
    return false;
}
```

---

## Bug 5：未设置 Executor 会假成功

原因：

```cpp
bool success = true;

if (executor_ != nullptr)
{
    ...
}
```

修复：

```text
run() 前检查 executor_ != nullptr
```

---

## Bug 6：按输入文件数计算任务依赖

原因：

```text
一个 producer 多输出
下游同时使用多个输出
```

修复：

```text
unique producer Edge
unique dependent Edge
```

---

## Bug 7：只靠日志判断并发正确性

原因：

```text
多线程和子进程输出可能乱序
```

修复：

```text
自动测试检查文件存在性、内容和退出码
```

---

# 三十、Day11 的并发基础知识

## 1. mutex

用于保护共享状态：

```text
ready_queue_
indegree_
completed_edges_
running_edges_
stopping_
```

任何多个线程可能同时访问的数据，都需要明确同步策略。

---

## 2. lock_guard

适合固定作用域加锁：

```cpp
{
    std::lock_guard<std::mutex> lock(
        mutex_
    );

    completed_edges_++;
}
```

离开作用域自动解锁。

---

## 3. unique_lock

条件变量需要：

```cpp
std::unique_lock<std::mutex> lock(
    mutex_
);
```

因为 `condition_variable::wait()` 需要在等待期间：

```text
自动释放 mutex
被唤醒后重新获取 mutex
```

---

## 4. condition_variable

避免 Worker 忙等：

错误方式：

```cpp
while (ready_queue_.empty())
{
    // 不断循环检查
}
```

这会浪费 CPU。

正确方式：

```cpp
condition_.wait(
    lock,
    predicate
);
```

没有任务时线程睡眠。

---

## 5. 谓词等待

推荐：

```cpp
condition_.wait(
    lock,
    [this]()
    {
        return stopping_
            || failed_
            || !ready_queue_.empty();
    }
);
```

原因是条件变量允许虚假唤醒。

线程醒来后必须重新检查真实条件。

---

## 6. notify_one 与 notify_all

当新加入一个任务时，理论上可以：

```cpp
notify_one();
```

停止或失败时，需要唤醒所有 Worker：

```cpp
notify_all();
```

当前实现较多使用 `notify_all()`，逻辑简单，但可能产生多余唤醒。后续可以优化。

---

## 7. 临界区最小化

锁内只做：

```text
共享状态读取
共享状态修改
队列操作
计数器操作
```

锁外做：

```text
g++ 编译
链接
磁盘操作
耗时计算
```

这是并发性能的关键。

---

# 三十一、Scheduler 的关键不变量

当前 Scheduler 应始终满足：

## 不变量 1

```text
jobs_ > 0
```

## 不变量 2

```text
executor_ != nullptr
```

## 不变量 3

每个 Ready Queue 中的 Edge：

```text
indegree == 0
```

## 不变量 4

每个 Edge 最多被入队一次。

## 不变量 5

下游只能在所有计划内 producer 完成后释放。

## 不变量 6

失败 Edge 不调用正常的 `finish_edge()`。

## 不变量 7

失败后不再领取新的任务。

## 不变量 8

```text
completed_edges_ <= total_edges_
```

## 不变量 9

```text
running_edges_ >= 0
```

## 不变量 10

任务依赖按唯一 Edge 计数，而不是按文件 Node 数量计数。

---

# 三十二、当前测试矩阵

| 测试                       | 验证内容                          |
| ------------------------ | ----------------------------- |
| `scheduler_guard`        | `jobs <= 0` 和未设置 Executor     |
| `scheduler_multi_output` | 单 producer 多输出、下游只释放一次        |
| `scheduler_failure`      | 上游失败、下游不执行、Scheduler 返回 false |
| 多源真实构建                   | 三个编译并行、link 最后执行              |
| `touch a.hpp`            | 一个头文件影响多个编译单元                 |
| `touch a.cpp`            | 最小重建 compile a + link         |
| `-j1 / -j3` 计时           | 验证实际并行加速                      |
| CTest TIMEOUT            | 并发死锁在 5 秒内失败                  |

---

# 三十三、常用验证命令

## 构建 ForgeBuild

```bash
cmake --build build/debug
```

## 正常并行构建

```bash
./build/debug/forge \
    -j 3 \
    build.forge
```

## 查看退出码

```bash
echo $?
```

## 查看测试列表

```bash
ctest \
    --test-dir build/debug \
    -N
```

## 运行全部测试

```bash
ctest \
    --test-dir build/debug \
    --output-on-failure
```

## 运行详细测试

```bash
ctest \
    --test-dir build/debug \
    -V
```

## 运行指定测试

```bash
ctest \
    --test-dir build/debug \
    -R scheduler_failure \
    -V
```

## 测量运行时间

```bash
/usr/bin/time \
    -f 'elapsed: %e seconds' \
    ./build/debug/forge \
    -j 3 \
    build.forge
```

---

# 三十四、面试时如何描述 Day11

可以这样说：

> 我为 ForgeBuild 实现了基于 DAG 的多线程并行调度器。Builder 先计算最小受影响构建子图，Scheduler 使用 Kahn 拓扑调度思想维护 Edge 入度和 Ready Queue，通过 Worker 线程池并行执行无依赖任务，并在前置任务完成后动态释放下游任务。

继续说明并发设计：

> 调度器使用 mutex 和 condition_variable 保护就绪队列、入度和运行状态。Worker 在锁内领取任务，在锁外执行编译命令，以缩小临界区。主线程和 Worker 使用不同条件变量分别等待任务到达和构建完成。

说明失败处理：

> 我实现了失败传播，任意命令失败后设置全局失败和停止状态，唤醒所有 Worker 以及等待构建完成的主线程，阻止失败任务的下游继续执行，并返回非零退出码。

说明发现并解决的问题：

> 在真实多源构建测试中，我发现 Parser 直接修改 Edge，未维护 Node 的反向生产者和消费者关系，导致链接任务入度被错误计算为零。我将建图统一收口到 BuildGraph 接口，保证 Edge 和 Node 双向关系一致。

说明特殊场景：

> 对于单个生产任务生成多个文件，而下游同时使用多个输出的情况，我将文件级依赖折叠为任务级依赖，使用集合对 producer 和 dependent Edge 对称去重，避免按输入文件数量错误计算入度。

说明测试：

> 我将调度器的非法参数、失败传播和多输出依赖场景接入 CTest，并为并发测试设置 5 秒超时，防止条件变量错误导致 CI 长时间挂死。同时通过构造三个独立的 1 秒任务，对比 `-j1` 和 `-j3`，测得约 2.46 倍加速。

---

# 三十五、简历中可以写成什么

可以压缩成一条：

> **并行 DAG 调度器：**基于 Kahn 拓扑调度实现 Worker 线程池、Ready Queue 与动态入度释放，支持 `-j N` 并行构建、最小增量子图调度和失败传播；处理单生产者多输出的任务依赖去重，修复条件变量漏通知导致的失败路径死锁，并接入带超时的 CTest 回归测试，三路独立任务实测获得约 2.46× 加速。

也可以拆成两条：

> **并行调度：**实现基于 DAG 入度和就绪队列的多线程构建调度器，支持 `-j N`、动态下游释放、增量子图执行和失败阻断。

> **可靠性验证：**解决双向建图缺失、失败路径条件变量漏通知及多输出依赖重复计数问题；使用 CTest 覆盖参数防御、失败传播和多输出场景，并设置超时防止并发死锁。

---

# 三十六、Day11 最终成果

Day11 开始前，ForgeBuild 已经能够：

```text
解析构建规则
判断增量脏状态
生成构建计划
串行执行命令
记录 BuildLog 和 DepsLog
```

Day11 完成后，进一步具备：

```text
命令行并行度控制
        +
多线程 Worker 池
        +
DAG 入度调度
        +
动态 Ready Queue
        +
最小增量子图并行执行
        +
失败传播与下游阻断
        +
条件变量正确唤醒
        +
非法状态内部防御
        +
任务级依赖去重
        +
CTest 自动回归
        +
并发测试超时保护
        +
真实并行性能验证
```

一句话总结：

> Day11 将 ForgeBuild 从“可以正确执行增量构建计划”升级成了“可以在依赖约束下可靠并行执行构建计划，并具备失败处理、并发健壮性和自动化回归测试”的构建引擎。
